'use strict';
// Staff leads — creates customer + runs pre-check on behalf of customer (same as dealer flow)
const { createLead } = require('../../dealer-app/controllers/leads.controller');
module.exports = { createLead };
